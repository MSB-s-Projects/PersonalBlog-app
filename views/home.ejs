<%- include("partials/header") %> 

<h1>Home</h1>
<p><%= homePara %> </p>
        
<%  posts.forEach((post) => { %>
    <h1><%= post.title %> </h1>
    <p><%= post.post.substring(0,100) + " ..." %> <a href="/posts/<%= _.kebabCase(post.title) %> ">Read More</a></p>
<%  }); %>

<%- include("partials/footer") %> 
